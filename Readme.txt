1> Get Resolution -->wd * ht

2> Create matrix M[wd][ht] = {{0}....}

3> while(1)
    3.1> M[i][j] = A*(sin(Ω*t + Φ)) 
        3.1.1> where Φ = f(x,y)
        3.1.2> *Try std::async , openCV, opengl

    3.2> dump M[i][j] in *.csv, *.json (* = 't')

 
4> Convert *.json to *.cvs to *.svg.
